<p style="color: green"><%= notice %></p>

<h1>Payments</h1>

<div id="payments">
  <% @payments.each do |payment| %>
    <%= render payment %>
    <p>
      <%= link_to "Show this payment", group_payment_path %>
    </p>
  <% end %>
</div>

<%= link_to "New payment", new_group_payment_path %>
